<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/AndroidManifest.xml -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission for QR scanning -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChowkingSmartDTR">

        <!-- Splash (launcher entry point) -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".ui.LoginActivity"
            android:windowSoftInputMode="adjustResize"/>

        <!-- Crew -->
        <activity android:name=".ui.crew.CrewHomeActivity"/>
        <activity android:name=".ui.crew.CrewHistoryActivity"/>

        <!-- Manager -->
        <activity android:name=".ui.manager.ManagerHomeActivity"/>
        <activity android:name=".ui.manager.ReportActivity"/>
        <activity android:name=".ui.manager.SalaryReportActivity"/>

        <!-- Admin -->
        <activity android:name=".ui.admin.AdminHomeActivity"/>

        <!--
            FileProvider — required for sharing CSV files on API 24+.
            authority must match: context.getPackageName() + ".fileprovider"
            The file_paths.xml defines which directories can be shared.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>
</manifest>